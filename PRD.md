# 浙江省走读式谈话间数据看板工具平台
## 产品需求文档（PRD）

**版本**：v1.0  
**日期**：2026-08-24  
**项目代号**：warning-dashboard  
**状态**：原型评审阶段

---

## 目录

1. [产品概述](#1-产品概述)
2. [用户角色与权限](#2-用户角色与权限)
3. [区域体系与数据范围](#3-区域体系与数据范围)
4. [功能模块一：顶部导航栏](#4-功能模块一顶部导航栏)
5. [功能模块二：四级联动筛选器](#5-功能模块二四级联动筛选器)
6. [功能模块三：统计卡片区](#6-功能模块三统计卡片区)
7. [功能模块四：柱状图（单位排行）](#7-功能模块四柱状图单位排行)
8. [功能模块五：饼图（预警类型分布）](#8-功能模块五饼图预警类型分布)
9. [功能模块六：折线图（趋势分析）](#9-功能模块六折线图趋势分析)
10. [功能模块七：预警列表](#10-功能模块七预警列表)
11. [功能模块八：预警详情弹窗](#11-功能模块八预警详情弹窗)
12. [功能模块九：复核操作弹窗](#12-功能模块九复核操作弹窗)
13. [核心业务逻辑](#13-核心业务逻辑)
14. [数据结构规范](#14-数据结构规范)
15. [预警类型字典](#15-预警类型字典)
16. [页面整体布局与交互流程](#16-页面整体布局与交互流程)
17. [非功能性需求](#17-非功能性需求)
18. [待确认事项](#18-待确认事项)

---

## 1. 产品概述

### 1.1 背景

浙江省纪检监察机关在全省各级谈话间开展走读式谈话工作。我司研发的**谛听产品**对谈话音视频进行 AI 分析，自动识别谈话过程中可能存在的违规行为并生成预警记录。

本平台通过接入**全省走读式谈话间数据库**及**谛听产品的预警分析结果**，为管理人员提供各区域谈话数据的汇总看板，并支持对谛听产品输出的预警进行人工复核——通过复核识别出误报，从而有针对性地反哺改进谛听产品的预警准确率。

### 1.2 产品定位

本平台是面向**省市区县三级纪检监察管理人员及谛听产品运营团队**使用的 Web 数据看板，提供以下核心能力：

- **谈话数据汇总**：接入全省走读式谈话间数据库，展示各区域谈话次数及分布情况
- **预警分析结果展示**：汇聚谛听产品输出的预警记录，展示预警次数、预警类型分布及趋势
- **预警质量评估**：通过预警正确率、误报率等指标，直观反映谛听产品在各区域的预警质量
- **区域下钻浏览**：支持从省级 → 地市 → 区县 → 谈话点 → 谈话间逐级下钻查看
- **人工复核闭环**：管理员对每条预警进行"正确预警/误报"复核，复核结果作为改进谛听产品的依据

### 1.3 数据来源说明

| 数据类型 | 来源 | 说明 |
|---------|------|------|
| 谈话记录 | 全省走读式谈话间数据库 | 谈话次数、时间、地点、谈话间信息 |
| 预警分析结果 | 谛听产品输出 | AI 识别的异常类型、时间、摘要、音视频片段 |
| 复核结论 | 本平台人工操作写回 | 正确预警 / 误报，复核人、复核时间、意见 |

### 1.4 技术实现现状

| 项目 | 现状 |
|------|------|
| 前端框架 | React 19 + Vite 8 + Tailwind CSS 4 |
| 图表库 | Recharts 3 |
| 路由 | 无（单页应用） |
| 状态管理 | React useState（集中在 App.jsx） |
| 数据来源 | 全量 Mock 数据（src/mockData.js） |
| 后端 API | 暂无，待规划（需对接谈话间数据库 + 谛听产品接口） |
| 认证体系 | 暂无，待规划 |

---

## 2. 用户角色与权限

> 📌 **截图占位区 — 用户角色示意图**
>
> _请在此处粘贴用户角色与系统关系图截图_

| 角色 | 描述 | 可见数据范围 | 核心操作 |
|------|------|-------------|---------|
| 省级管理员 | 省纪委监委机关人员 | 全省所有区域 | 查看统计、执行复核 |
| 市级管理员 | 各地市纪委人员 | 本市下属区域 | 查看统计、执行复核 |
| 区县管理员 | 区县纪委人员 | 本区县下属谈话点 | 查看统计、执行复核 |
| 产品运营人员 | 谛听产品团队 | 全省（只读） | 查看误报分布，分析预警质量 |
| 只读观察员 | 监督人员 | 授权范围 | 仅查看 |

> ⚠️ 当前版本无登录验证，`reviewPerson` 硬编码为"当前用户"，正式版需接入统一身份认证。

---

## 3. 区域体系与数据范围

### 3.1 区域层级结构

本平台采用五层区域体系：

```
省（浙江省）
  ├── 浙江省本级（省厅直属谈话点）
  └── 地市（11个）
        ├── {市名}本级（市本级谈话点）
        └── 区县
              ├── {区县名}本级（区本级谈话点）
              └── 普通乡镇/街道谈话点（部分区县有）
```

### 3.2 覆盖地市（11个）

杭州市 / 宁波市 / 温州市 / 绍兴市 / 金华市 / 嘉兴市 / 湖州市 / 台州市 / 衢州市 / 丽水市 / 舟山市

### 3.3 筛选器的特殊路径说明

**路径 A — 省本级路径**：
```
地市选「浙江省本级」→ 跳过区县层 → 直接选择省本级谈话点 → 谈话间
```

**路径 B — 市本级路径**：
```
地市选「杭州市」→ 区县选「杭州市本级」→ 直接选择市本级谈话点 → 谈话间
```

**路径 C — 区本级路径**：
```
地市选「杭州市」→ 区县选「滨江区」→ 区选「滨江区本级」→ 谈话点 → 谈话间
```

**路径 D — 普通区县直通路径**（特殊逻辑）：
```
地市选「杭州市」→ 区县选「滨江区」→ 若直接选择"滨江区"（非本级）→ 跳过谈话点层 → 直接到谈话间
```

> ⚠️ 此路径 D 的逻辑待产品确认：普通区县是否应跳过谈话点层级。

---

## 4. 功能模块一：顶部导航栏

### 4.1 功能描述

页面最顶部固定的品牌/导航区域，蓝色渐变背景，提供系统标题和数据更新时间。

### 4.2 UI 要素

| 要素 | 内容 | 说明 |
|------|------|------|
| 系统标题 | 浙江省走读式谈话间数据看板工具平台 | 白色文字，居左 |
| 数据更新时间 | 数据更新：2026-08-20 12:00 | 白色小字，居右 |
| 背景 | 蓝色渐变（`#1e3a5f` → `#2563EB`） | 全宽 |

### 4.3 交互说明

- 无可点击交互，纯展示
- 固定在页面顶部，滚动时不消失

> 📌 **截图占位区 — 顶部导航栏**
>
> _请在此处粘贴顶部导航栏截图_

---

## 5. 功能模块二：四级联动筛选器

### 5.1 功能描述

位于导航栏下方的筛选条，提供省→市→区县→谈话点→谈话间四级区域联动筛选。筛选结果作用于页面所有数据展示模块（统计卡片、图表、预警列表）。

### 5.2 UI 要素

| 控件 | 描述 | 默认值 |
|------|------|-------|
| 地市下拉框 | 显示全部地市 + 浙江省本级 | 全部 |
| 区县下拉框 | 根据选中地市动态加载；省本级时**隐藏** | 全部 |
| 谈话点下拉框 | 根据选中区县动态加载；普通区县时**隐藏** | 全部 |
| 谈话间下拉框 | 根据选中谈话点动态加载 | 全部 |
| 确定按钮 | 提交筛选条件，触发全局数据刷新 | — |
| 刷新按钮 | 硬刷新页面（`window.location.reload()`） | — |

### 5.3 联动规则

1. 地市切换 → 区县重置为"全部" → 谈话点重置 → 谈话间重置
2. 区县切换 → 谈话点重置 → 谈话间重置
3. 谈话点切换 → 谈话间重置
4. 只有点击"确定"后，筛选条件才真正生效（非实时）
5. 筛选器显示的是**临时选中状态**，已确认的状态由父组件维护

### 5.4 特殊显隐逻辑

| 场景 | 区县框 | 谈话点框 | 谈话间框 |
|------|-------|---------|---------|
| 默认（全选） | 显示 | 显示 | 显示 |
| 选择「浙江省本级」 | **隐藏** | 显示（省本级谈话点） | 显示 |
| 选择普通区县 | 显示 | **隐藏** | 显示（直通谈话间） |
| 选择「XX市本级」 | 显示 | 显示（市本级谈话点） | 显示 |

> 📌 **截图占位区 — 筛选器（默认状态）**
>
> _请在此处粘贴筛选器默认展开状态截图_

> 📌 **截图占位区 — 筛选器（省本级路径）**
>
> _请在此处粘贴选择「浙江省本级」后筛选器样式截图_

> 📌 **截图占位区 — 筛选器（普通区县路径）**
>
> _请在此处粘贴选择普通区县后谈话点隐藏的截图_

---

## 6. 功能模块三：统计卡片区

### 6.1 功能描述

位于筛选器下方的统计卡片区，展示当前筛选范围内的核心数字指标。所有数字从谈话间数据库及谛听预警结果中汇总，根据筛选条件动态计算。

### 6.2 四张卡片定义

| 卡片 | 指标名称 | 计算逻辑 | 图标色 |
|------|---------|---------|-------|
| 卡片 1 | 预警总数 | 谛听产品在当前筛选范围内输出的预警记录总条数 | 蓝色 |
| 卡片 2 | 待处理 | `reviewStatus === '未复核'` 的预警条数 | 橙色 |
| 卡片 3 | 已处理 | `reviewStatus === '正确预警' 或 '误报'` 的预警条数 | 绿色 |
| 卡片 4 | 误报数 | `reviewStatus === '误报'` 的条数，附加**误报率** | 红色 |

> 📝 **待扩展**：正式版建议增加「谈话总次数」卡片（来自谈话间数据库），以便同时展示谈话次数和预警次数，计算**预警触发率**（预警次数 / 谈话次数）。

### 6.3 误报率与预警正确率计算

```
误报率     = 误报数 / 已处理数 × 100%
预警正确率 = 正确预警数 / 已处理数 × 100%
（已处理数为 0 时均显示 0.0%）
```

> 📝 **产品价值**：误报率是衡量谛听产品预警质量的核心指标。通过人工复核积累的误报数据，可反哺谛听模型迭代，有针对性地降低特定类型或特定区域的误报率。

### 6.4 数字格式

- 大号数字（主指标），字号约 2rem
- 副文字说明（如误报率显示在误报数下方）

> 📌 **截图占位区 — 统计卡片区（省级全量数据）**
>
> _请在此处粘贴四张统计卡片截图（筛选器为全部时）_

> 📌 **截图占位区 — 统计卡片区（下钻到具体区县）**
>
> _请在此处粘贴筛选到具体区县后的统计卡片截图_

---

## 7. 功能模块四：柱状图（单位排行）

### 7.1 功能描述

展示当前筛选层级下各下级单位的**预警次数排行**，帮助管理人员快速定位高预警频率区域，支持点击柱条下钻到下一层级。

### 7.2 显示逻辑（根据筛选层级动态切换）

| 当前筛选层级 | 柱状图 X 轴内容 | 示例 |
|------------|--------------|------|
| 全部（无筛选） | 各地市预警数排行 | 杭州市 / 宁波市 / ... |
| 已选地市 | 该市下属区县排行 | 滨江区 / 萧山区 / ... |
| 已选区县 | 该区县下属谈话点排行 | 长河路点 / 西兴路点 |
| 已选谈话点 | 该谈话点下属谈话间排行 | CH-01室 / CH-02室 |
| 已选谈话间 | **隐藏柱状图**，仅显示饼图 | — |

### 7.3 点击下钻交互

- 点击某个柱条 → 自动将筛选器下钻到对应子单位
- 联动更新：筛选器状态 + 统计卡片 + 图表 + 列表同步刷新

### 7.4 图表样式

- 横向显示：左侧为 Y 轴（单位名），右侧为 X 轴（数量）
- 柱条悬停 Tooltip 显示具体数值
- 颜色：蓝色系（`#3B82F6`）

> 📌 **截图占位区 — 柱状图（地市排行）**
>
> _请在此处粘贴省级视图下的地市预警数排行柱状图截图_

> 📌 **截图占位区 — 柱状图（点击下钻后）**
>
> _请在此处粘贴点击某地市后下钻到区县排行的截图_

---

## 8. 功能模块五：饼图（预警类型分布）

### 8.1 功能描述

展示当前筛选范围内，8 种预警类型各自占比的圆环饼图。

### 8.2 数据来源

从筛选后的预警列表中，按 `type`（001–008）分组统计，动态计算各类型数量和占比。

### 8.3 显示规则

- 当筛选至**具体谈话间**时（`hideChart = true`）：柱状图隐藏，饼图**独占全宽**显示
- 其他层级：饼图与柱状图**并列显示**（各占约 50% 宽度）

### 8.4 图表样式

- 圆环图（Donut Chart）样式
- 图例显示所有类型及对应颜色
- 悬停 Tooltip 显示类型名、数量、占比
- 8 种类型对应 8 种不同颜色

> 📌 **截图占位区 — 饼图（与柱状图并列）**
>
> _请在此处粘贴柱图+饼图并列显示的截图_

> 📌 **截图占位区 — 饼图（独占全宽，谈话间层级）**
>
> _请在此处粘贴筛选到具体谈话间后饼图全宽展示的截图_

---

## 9. 功能模块六：折线图（趋势分析）

### 9.1 功能描述

展示当前筛选范围内谛听产品**预警次数随时间的变化趋势**，辅助判断预警高发时段及产品输出稳定性，支持切换 3 种时间维度。

### 9.2 时间维度切换

| Tab | 时间范围 | X 轴粒度 |
|-----|---------|---------|
| 近7天 | 最近 7 日 | 按天 |
| 近30天 | 最近 30 日 | 按天 |
| 近90天 | 最近 90 日 | 按天 |

默认选中：近 7 天。

### 9.3 数据计算逻辑

折线图数据从 `filteredWarnings`（当前筛选后的预警列表）中**动态按日期聚合**生成，而非固定静态数据。每个日期点统计该日期下的预警总数。

### 9.4 图表样式

- 折线图（Line Chart）
- X 轴：日期，Y 轴：预警数量
- 数据点悬停 Tooltip 显示日期和数量
- 线条颜色：蓝色（`#3B82F6`）
- Tab 切换按钮位于图表右上方

> 📌 **截图占位区 — 折线图（近7天）**
>
> _请在此处粘贴折线图近7天视图截图_

> 📌 **截图占位区 — 折线图（近30天）**
>
> _请在此处粘贴折线图近30天视图截图_

---

## 10. 功能模块七：预警列表

### 10.1 功能描述

分页展示当前筛选范围内的全部预警记录，支持列表内二次筛选（需点击确定后生效），可对每条记录进行详情查看和复核操作。

### 10.2 列表字段（8列）

| 列名 | 字段 | 说明 |
|------|------|------|
| 序号 | 自增行号 | 当前页码内的行序 |
| 谈话间号 | `talkingRoom` | 如 CH-01室 |
| 谈话地点 | `location` + `town` 组合 | 如 浙江/杭州市/滨江区·长河路点 |
| 异常内容 | `typeName` | 如 不文明用语 |
| 异常时间 | `time` | 格式：YYYY-MM-DD HH:mm:ss |
| 复核状态 | `reviewStatus` | 未复核 / 正确预警 / 误报（带颜色标签） |
| 复核人员 | `reviewPerson` | 空或人员姓名 |
| 复核时间 | `reviewTime` | 空或时间字符串 |
| 操作 | — | 查看按钮 + 复核按钮（或已复核标记） |

### 10.3 操作按钮规则

| 场景 | 查看按钮 | 复核按钮 |
|------|---------|---------|
| `reviewStatus === '未复核'` | 显示，可点击 | **显示"复核"，可点击** |
| `reviewStatus === '正确预警'` | 显示，可点击 | 显示"已复核"，不可点击（灰色） |
| `reviewStatus === '误报'` | 显示，可点击 | 显示"已复核"，不可点击（灰色） |

### 10.4 列表内二次筛选

位于列表表格上方，提供 4 个筛选条件：

| 筛选项 | 控件类型 | 选项 | 特殊规则 |
|-------|---------|------|---------|
| 是否复核 | 下拉框 | 全部 / 未复核 / 已复核 | — |
| 是否属实 | 下拉框 | 全部 / 正确预警 / 误报 | 选"未复核"时此框自动 disabled，显示"--" |
| 异常内容 | 下拉框 | 全部 + 8种预警类型 | — |
| 谈话日期 | 日期范围选择（开始日期 / 结束日期） | — | — |
| 确定按钮 | 按钮 | — | 点击后筛选条件才生效 |

> ⚠️ **重要规则**：列表内筛选与顶部筛选器是**叠加关系**，列表筛选在已筛选数据范围内再次过滤。

### 10.5 "是否属实"联动规则

```
当 filterReviewStatus === '未复核' 时：
  → filterValidity 下拉框 disabled
  → 显示占位文字 "--"
  → 已选值清空
```

### 10.6 分页规则

- 每页显示 **10 条**记录
- 分页器最多显示 **7 个页码按钮**，超出时显示省略号（`...`）
- 省略号规则：首末页始终显示，中间动态计算窗口
- 页码格式示例：`1 ... 4 5 6 ... 20`
- 翻页后自动回到列表顶部

### 10.7 复核状态标签样式

| 状态 | 标签样式 |
|------|---------|
| 未复核 | 灰色背景，灰色文字 |
| 正确预警 | 绿色背景，绿色文字 |
| 误报 | 红色背景，红色文字 |

> 📌 **截图占位区 — 预警列表（默认视图）**
>
> _请在此处粘贴预警列表完整截图（含表头、数据行、分页）_

> 📌 **截图占位区 — 预警列表（使用列表内筛选后）**
>
> _请在此处粘贴使用列表内筛选条件筛选后的效果截图_

> 📌 **截图占位区 — 预警列表（混合复核状态）**
>
> _请在此处粘贴同时包含「未复核」「正确预警」「误报」三种状态行的截图_

---

## 11. 功能模块八：预警详情弹窗

### 11.1 触发方式

点击预警列表中任意行的"**查看**"按钮，弹出该预警的详情弹窗。

### 11.2 展示字段

| 字段 | 数据来源 | 说明 |
|------|---------|------|
| 谈话地点 | `location` | 如 浙江/杭州市/滨江区 |
| 谈话间号 | `talkingRoom` | 如 CH-01室 |
| 异常内容 | `typeName` | 如 不文明用语 |
| 异常时间 | `time` | 完整时间戳 |
| 预警摘要 | `content` | AI 生成的异常描述文本 |
| 音视频记录 | `audio` | 显示音频播放器 UI |

### 11.3 音视频播放器

- **当前实现**：仅 UI 展示，不实现真实播放功能
- 显示内容：播放/暂停按钮图标（▶）+ 进度显示（`00:15:32 / 00:45:20`）
- 标签文字："谈话录音"
- 正式版需接入真实音视频流

### 11.4 关闭方式

- 右上角 ✕ 按钮
- 底部"关闭"按钮
- 点击弹窗遮罩层外区域（待确认是否支持）

> 📌 **截图占位区 — 预警详情弹窗**
>
> _请在此处粘贴预警详情弹窗完整截图_

---

## 12. 功能模块九：复核操作弹窗

### 12.1 触发方式

点击预警列表中 `reviewStatus === '未复核'` 行的"**复核**"按钮，弹出复核操作弹窗。

### 12.2 展示字段（只读，供复核人参考）

| 字段 | 数据来源 |
|------|---------|
| 谈话地点 | `location` |
| 谈话间号 | `talkingRoom` |
| 异常内容 | `typeName` |
| 异常时间 | `time` |

### 12.3 操作字段

| 字段 | 控件 | 必填 | 选项 |
|------|------|------|------|
| 复核结果 | 单选框（Radio） | **必填** | 正确预警 / 误报 |
| 复核意见 | 多行文本框 | 选填 | 自由文本，最大长度待定 |

### 12.4 提交逻辑

```
若未选复核结果：
  → alert('请选择复核结果')
  → 不提交

若已选复核结果：
  → 写入：
    reviewStatus = '正确预警' 或 '误报'
    reviewPerson = '当前用户'（待替换为登录人姓名）
    reviewTime = 当前系统时间（格式：YYYY-MM-DD HH:mm:ss）
    reviewOpinion = textarea 输入值
  → 更新 warnings 列表对应 id 的条目（前端本地状态更新）
  → 关闭弹窗
  → 列表对应行实时刷新显示新的复核状态/人员/时间
```

### 12.5 关闭方式

- 右上角 ✕ 按钮
- 底部"取消"按钮
- 提交成功后自动关闭

> 📌 **截图占位区 — 复核弹窗（未填写）**
>
> _请在此处粘贴复核弹窗初始状态截图_

> 📌 **截图占位区 — 复核弹窗（已选正确预警）**
>
> _请在此处粘贴选择「正确预警」后的弹窗截图_

> 📌 **截图占位区 — 复核弹窗（已选误报）**
>
> _请在此处粘贴选择「误报」后的弹窗截图_

> 📌 **截图占位区 — 复核提交后列表更新**
>
> _请在此处粘贴复核完成后列表行状态实时更新的截图_

---

## 13. 核心业务逻辑

### 13.1 全局筛选 → 数据联动

```
用户操作筛选器 → 点击"确定"
  ↓
App.jsx getFilteredWarnings() 重新计算 filteredWarnings
  ↓
同步触发以下模块更新：
  ├── 统计卡片：重新计算 4 个指标
  ├── 柱状图：重新计算各下级单位预警数
  ├── 饼图：重新计算 8 种类型分布
  ├── 折线图：重新按日期聚合计算趋势
  └── 预警列表：重置到第1页，显示新数据
```

### 13.2 筛选器区域过滤算法

```javascript
// 核心过滤逻辑（伪代码）

// 场景一：省本级路径
if (selectedCity === '浙江省本级') {
  filter: w.location.split('/')[1] === '浙江省本级'
  if (selectedTown !== '全部') further filter by town
  if (selectedRoom !== '全部') further filter by talkingRoom
}

// 场景二：普通区县路径（选了区县但没选谈话点）
else if (selectedDistrict不以'本级'结尾) {
  // 谈话点层被跳过，town 字段自动匹配为 `${district}本级`
  filter: w.location 包含 city 且 district
  if (selectedRoom !== '全部') further filter by talkingRoom
}

// 场景三：标准路径
else {
  if (selectedCity !== '全部') filter by city
  if (selectedDistrict !== '全部') filter by district
  if (selectedTown !== '全部') filter by town
  if (selectedRoom !== '全部') filter by talkingRoom
}
```

### 13.3 柱状图动态 X 轴逻辑

```javascript
// 根据当前选中层级决定图表显示内容（伪代码）

if (selectedRoom !== '全部') → hideChart = true（不显示柱状图）
else if (selectedTown !== '全部') → X轴=各谈话间
else if (selectedDistrict !== '全部') → X轴=各谈话点
else if (selectedCity !== '全部') → X轴=各区县
else → X轴=各地市
```

### 13.4 柱状图点击下钻逻辑

```javascript
handleChartDetailClick(item) {
  // item.name 是被点击的单位名称
  // 根据当前层级，将对应的筛选器状态更新为 item.name
  
  if (当前显示地市排行) → setSelectedCity(item.name)
  else if (当前显示区县排行) → setSelectedDistrict(item.name)
  else if (当前显示谈话点排行) → setSelectedTown(item.name)
  else if (当前显示谈话间排行) → setSelectedRoom(item.name)
}
```

### 13.5 复核工作流全程状态变化

```
初始状态：reviewStatus = '未复核'
         reviewPerson = ''
         reviewTime = ''
         reviewOpinion = ''

点击复核 → ReviewModal 弹出

选择「正确预警」并提交：
  → reviewStatus = '正确预警'
  → reviewPerson = '当前用户'（后期替换）
  → reviewTime = '2026-08-24 10:30:00'
  → reviewOpinion = '用户填写的意见'

选择「误报」并提交：
  → reviewStatus = '误报'
  → 其余字段同上

复核后：
  → 列表行"复核"按钮变为"已复核"（灰色不可点）
  → 复核状态标签颜色变化（未复核灰→正确预警绿/误报红）
  → 待处理数 -1，已处理数 +1（统计卡片实时更新）
```

### 13.6 列表内二次筛选规则

```
已通过顶部筛选器得到 filteredWarnings（基础数据集）
  ↓
点击列表内"确定"后，在基础数据集上叠加过滤：

  if (appliedFilters.reviewStatus !== '全部')
    filter: reviewStatus 匹配
  
  if (appliedFilters.validity !== '全部' && appliedFilters.reviewStatus !== '未复核')
    filter: reviewStatus === appliedFilters.validity
  
  if (appliedFilters.warningType !== '全部')
    filter: typeName === appliedFilters.warningType
  
  if (appliedFilters.dateStart 存在)
    filter: time >= dateStart
  
  if (appliedFilters.dateEnd 存在)
    filter: time <= dateEnd + '23:59:59'
```

---

## 14. 数据结构规范

### 14.1 预警条目（WarningItem）

```typescript
interface WarningItem {
  id: string;              // "W0001" 格式，唯一标识
  time: string;            // "2026-08-16 10:21:37" 格式
  location: string;        // "浙江/杭州市/滨江区" 三段路径
  town: string;            // 谈话点名称，如 "长河路点"
  talkingRoom: string;     // 谈话间编号，如 "CH-01室"
  type: string;            // 类型编码 "001"–"008"
  typeName: string;        // 类型中文名
  content: string;         // 预警摘要文本
  audio: string;           // 音视频资源路径
  reviewStatus: '未复核' | '正确预警' | '误报';
  reviewPerson: string;    // 复核人姓名，空字符串=未复核
  reviewTime: string;      // 复核时间，空字符串=未复核
  reviewOpinion: string;   // 复核意见，可为空
  
  // 兼容字段（旧版，待清理）
  misreportTag: boolean;
  status: '未标注' | '已标注-非误报' | '已标注-误报';
}
```

### 14.2 区域层级结构

```typescript
interface AreaData {
  cities: string[];                       // 地市列表（含"浙江省本级"）
  districts: Record<string, string[]>;   // 地市 → 区县列表
  towns: Record<string, string[]>;       // 区县 → 谈话点列表
  talkingRooms: Record<string, string[]>; // 谈话点 → 谈话间列表
}
```

### 14.3 复核提交数据

```typescript
interface ReviewSubmitData {
  warningId: string;
  reviewStatus: '正确预警' | '误报';
  reviewOpinion: string;
  reviewPerson: string;   // 当前登录用户姓名
  reviewTime: string;     // 提交时的系统时间
}
```

### 14.4 列表筛选条件

```typescript
interface ListFilters {
  reviewStatus: '全部' | '未复核' | '已复核';
  validity: '全部' | '正确预警' | '误报';
  warningType: '全部' | string;   // 类型名称或"全部"
  dateStart: string;              // 开始日期 YYYY-MM-DD
  dateEnd: string;                // 结束日期 YYYY-MM-DD
}
```

---

## 15. 预警类型字典

| 编码 | 类型名称 | 描述 |
|------|---------|------|
| 001 | 不文明用语 | 谈话过程中使用侮辱、歧视性言辞 |
| 002 | 威胁恐吓 | 对被谈话人进行威胁、恐吓 |
| 003 | 程序告知不充分 | 未充分告知被谈话人权利义务 |
| 004 | 指供诱供 | 引导或诱导被谈话人按预设内容陈述 |
| 005 | 违规承诺 | 作出超越权限的承诺 |
| 006 | 权益未保障 | 被谈话人合法权益未得到保障 |
| 007 | 自伤自杀倾向 | 被谈话人出现自伤或自杀倾向信号 |
| 008 | 其他 | 其他异常情况 |

---

## 16. 页面整体布局与交互流程

### 16.1 页面垂直布局（从上至下）

```
┌─────────────────────────────────────────────┐
│  顶部导航栏（固定，蓝色渐变）                     │
├─────────────────────────────────────────────┤
│  四级联动筛选器（sticky，白色背景，带阴影）         │
├─────────────────────────────────────────────┤
│  统计卡片区（4列网格）                          │
│  [预警总数] [待处理] [已处理] [误报数+误报率]      │
├───────────────────────┬─────────────────────┤
│  柱状图（单位排行）      │  饼图（类型分布）     │
│  （筛选到谈话间时隐藏）  │  （始终显示）         │
├─────────────────────────────────────────────┤
│  折线图（趋势分析）                             │
│  [近7天] [近30天] [近90天] Tab切换             │
├─────────────────────────────────────────────┤
│  预警列表                                     │
│  ┌─ 列表内筛选栏 ────────────────── [确定] ─┐  │
│  │  是否复核 | 是否属实 | 异常内容 | 日期范围 │  │
│  └────────────────────────────────────────┘  │
│  ┌─ 数据表格 ─────────────────────────────┐  │
│  │  序号 谈话间号 地点 类型 时间 状态 人员 时间 操作│ │
│  │  ...行数据...                           │  │
│  └────────────────────────────────────────┘  │
│  ┌─ 分页器 ──────────────────────────────┐  │
│  │     [< 1 ... 5 6 7 ... 20 >]          │  │
│  └────────────────────────────────────────┘  │
└─────────────────────────────────────────────┘
```

### 16.2 核心用户操作流程

```
进入页面（省级全量视图）
  ↓
① 通过筛选器缩小范围（可选）
  地市 → 区县 → 谈话点 → 谈话间 → [确定]
  ↓
② 查看统计概览
  观察4张卡片数字 + 柱状图排行
  ↓
③ 可选：点击柱状图条目下钻
  自动更新筛选器到下一层级
  ↓
④ 查看趋势
  切换折线图时间范围（7/30/90天）
  ↓
⑤ 处理预警列表
  可选：使用列表内筛选缩小列表范围
  ↓
⑥-A：查看详情
  点击"查看" → WarningModal弹出 → 查看内容/音频
  ↓
⑥-B：执行复核
  点击"复核" → ReviewModal弹出
  选择"正确预警"或"误报" + 填写意见
  点击提交 → 弹窗关闭 → 列表行实时更新
```

> 📌 **截图占位区 — 完整页面截图（省级视图）**
>
> _请在此处粘贴进入页面后的完整截图（含导航栏、筛选器、卡片、图表、列表）_

> 📌 **截图占位区 — 完整页面截图（下钻到某区县）**
>
> _请在此处粘贴筛选到具体区县后的完整页面截图_

---

## 17. 非功能性需求

### 17.1 性能要求

| 指标 | 要求 |
|------|------|
| 页面首次加载 | ≤ 3 秒（网络正常） |
| 筛选器切换响应 | ≤ 500ms |
| 图表渲染 | ≤ 1 秒 |
| 列表翻页 | ≤ 300ms |

### 17.2 兼容性要求

| 浏览器 | 要求 |
|-------|------|
| Chrome | ≥ 90 |
| Edge | ≥ 90 |
| Firefox | ≥ 88 |
| Safari | ≥ 14 |

### 17.3 分辨率要求

- 最小支持分辨率：1280 × 768
- 推荐分辨率：1920 × 1080
- 当前版本：仅适配桌面端，不支持移动端

### 17.4 安全要求（正式版规划）

- 必须接入统一身份认证（SSO/LDAP）
- 用户仅能查看本权限范围内的数据
- 所有数据传输使用 HTTPS
- 音视频文件访问需要权限控制
- 操作日志留存（复核操作必须可追溯）

### 17.5 可用性要求

- 系统可用性：≥ 99.5%（工作时间内）
- 数据刷新频率：建议 5–10 分钟自动刷新一次（当前版本无此能力）

---

## 18. 待确认事项

| 编号 | 问题 | 影响模块 | 优先级 |
|------|------|---------|-------|
| Q1 | 普通区县直通谈话间的路径 D 逻辑是否为预期行为？ | 筛选器 | 高 |
| Q2 | 复核后的数据是否需要同步到后端？当前仅更新前端本地状态。 | 复核弹窗 | 高 |
| Q3 | `reviewPerson` 应从登录用户信息中获取，用户体系如何接入？ | 复核弹窗 | 高 |
| Q4 | 音视频文件如何存储和访问？当前为模拟路径。 | 详情弹窗 | 高 |
| Q5 | 数据刷新策略：轮询/WebSocket/手动刷新？ | 全局 | 中 |
| Q6 | 误报率公式中分母是"已处理"还是"预警总数"？ | 统计卡片 | 中 |
| Q7 | 列表"已复核"状态是否应允许再次复核（修改复核结论）？ | 预警列表 | 中 |
| Q8 | 折线图趋势数据是从预警列表前端聚合，还是独立 API 提供？ | 折线图 | 中 |
| Q9 | 是否需要导出功能（Excel/PDF）？ | 预警列表 | 低 |
| Q10 | 是否需要按预警类型下钻到具体预警的功能？ | 饼图 | 低 |
| Q11 | 旧字段 `misreportTag` 和 `status` 是否可以清除？ | 数据结构 | 低 |
| Q12 | 弹窗是否应支持点击遮罩关闭？ | 两个弹窗 | 低 |

---

*文档结束*

> 📌 **截图占位区 — 其他截图**
>
> _如有其他需要记录的截图，请在此处补充_

